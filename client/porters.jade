template(name='porters')
    .ui.stackable.padded.grid
        .sixteen.wide.column
            .ui.header
                i.hand.holding.icon
                |porters
            each porter_tasks
                .ui.card.log_work
                    .ui.header #{title}
    